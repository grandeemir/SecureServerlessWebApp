name: Terraform CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  TF_VERSION: "1.8.5"
  WORKING_DIR: "environments/dev"
  AWS_REGION: "us-east-1"

jobs:
  terraform:
    name: Terraform Validate and Plan
    runs-on: ubuntu-latest

    steps:
      # 1. Kök dizinde sorunsuz çalışması için working-directory bağımlılığı yok
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      # OIDC bağlantısı kök dizinden bağımsız çalışır
      - name: Configure AWS Credentials with OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::653853166958:role/github-actions-terraform-role
          aws-region: ${{ env.AWS_REGION }}

      # 2. Tüm depodaki kod biçimlendirmesini kontrol etmek için kök dizinde çalışır
      - name: Terraform Format
        run: terraform fmt -check -recursive

      # 3. Altyapı komutları sadece ilgili environment klasöründe çalışır
      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform init -input=false

      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform validate

      - name: Terraform Plan
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform plan -input=false